在按alt+F7就回到字符界面了

在纯字符 模式下多爽啊 alt+ctrl+shift+Fx

切回图形界面
 
startx

搜索当前目录下所有包含XXX这个词的普通文件
#find . -name \* -type f -print | xargs grep "XXX"

比如目录/zc下有多个子目录a,b,c.而各个目录下又有文件比如 a1,a2,a3;b1,b2,b3;c1.我知道我想找的文件里有"wow" 用什么语句可以把我想找的文件快速找到呢
       最佳答案 最简单的方法
grep -R 'wow' /zc

如何你装的是redhat系统的操作系统，如centos,fedora
你可以直接命令行下，执行yum install gcc* -y 
这个会把gcc相关的包全部下载安装

命令历史与命令重复
# hsitory
  1 ls
  2 pwd
再次执行命令 # !命令编号 --(# !1)

# !pw 该命令将在历史命令的列表中查找匹配的命令并执行 (pw为pwd命令的部分)

快捷键  ctrl+U: 快速删除光标之前的所有内容
	ctrl+K: 快速删除光标之后至行尾的所有内容
	ctrl+L: 清屏

递归创建文件夹
mkdir -p dir1/dir2/dir3
递归删除文件夹
rm -rf root_dir   (-r:递归删除 -f:不提示Yes or No)

 5.查找与替换操作(末行模式)

 (1).自上而下：":/word"查找指定的字符串，"n"查找下一个（自上而下）

                                         "N"反向查找下一个（自下而上）
 (2).自下而上：":?word"自下而上查找指定字符串，"n"查找下一个（自下而上）

                                               "N"反向查找下一个（自上而下）
 (3).普通替换：":s/old/new"由s开始，"old"指被替换的字符串，"new"替换成哪个字符串，

                 此命令只替换当前行中第一个匹配的字符串
 (4).行内全部替换：":s/old/new/g"，当前行内所有匹配的字符串
 (5).在行区域内进行替换：":#,#s/old/new/g"，"#,#"表示两个行号之间的行区域，

                          例如"3,5"表示第3行到第5行
 (6).整个文件内的替换操作：":%s/old/new/g"，加%表示整篇文档，危险，慎用

#ps aux 查看所有进程
#top 类似任务管理器
